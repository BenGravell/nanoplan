# Simulation

Contains the vehicle state/control types, kinematic integration, command limits, dynamic collision handling, and the `Simulator` ego plant used by both the live world and planner tests.

The realtime application orchestration (track, traffic, planner, and diagnostics) lives in `world`.
